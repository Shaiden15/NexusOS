{
  "buildCommand": "npm run build",
  "ignoreCommand": "npx --yes vite --mode production --help > NUL",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
